@import nib;

global-reset();

@import colors;
@import typography;

html {
  height: 100%;
}

body {
  background-color: c_steel;
  height: 100%;
}

.main-interactive {
  max-width: 960px;
  margin: 0 10px;
}

.credits {
  background-color: rgba(255,255,255,.6);
  padding: 10px 20px;
}

#interactive {
  width: 100%;
}

.pinger {
  line {
    stroke-weight: 0.5;
    stroke: c_grey_2;
  }
}
